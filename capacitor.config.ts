import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tnyfood.notadigital',
  appName: 'Nota Digital TNY',
  webDir: 'dist'
};

export default config;
